pipeline {
    agent any

    stages {        
        stage('Run Automation Test Cases') {
            steps {
                script {
                    sh "echo $BRANCH"
                    sh "echo ${BRANCH}"
                }
            }
        }
    }
}
